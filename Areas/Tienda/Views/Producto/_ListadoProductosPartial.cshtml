
@model IEnumerable<Market.Areas.Tienda.ViewModels.ProductosViewModel>


@if (Model.Count() == 0)
{
    <p>No hay productos disponibles.</p>
}
else
{
    <div class="row">
        @foreach (var item in Model)
        {
            <h1>@item.Nombre</h1>

        }
    </div>
}
